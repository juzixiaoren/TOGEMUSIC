帮我写提示词，我要用这个提示词给能对我项目进行操作的AI，让他帮我用前端vue(帮我写页面）（后端flask调用python代码），实现1.用户登录注册（使用hash保存密文作为密码）2.用户可以上传音乐（自动提取文件名和属性中歌手名字），上传后的显示框会显示提取的文件名作为歌名，歌手名字作为歌手（可以批量上传），这个显示框可以修改，用户可以自己修改歌名和歌手名（批量修改，并非传一个改一个，而是批量上传完后用户可以对一堆框进行修改（文件名.文件类型-歌名(默认文件名)-歌手名（后两个可以修改）））。然后用户点击确认后正式存储在本地，并存储进数据库。3.用户可以创建歌单（输入歌单名字），点击确认后存储进数据库。4.用户可以点进歌单详细页，可以导入歌曲（用户可以查看后台所有上传的歌曲（无论是自己上传还是别人上传），并可以根据（上传时间），上传用户名进行筛选，可以进行歌手/歌曲的搜索筛选，并勾选（可多选）最后点击确认将这些歌曲导入歌单（注意：切换筛选条件不会导致已经勾选的歌曲取消勾选）），此外，用户还可以查看所有歌单页，多选歌单导入本歌单（自动去重）。5.用户可以进入主房间（只有一个房间，做成固定页面，无需保存在数据库），并从歌单页or音乐页选择歌单(自动勾选歌单中全部音乐，可以一键取消勾选然后自己单选），选择音乐（同上面的筛选功能）加入正在播放的歌曲列表（正在播放歌单是数据库中的歌单表中的ID=1的表，不允许删除歌单（但是允许删除歌曲，可以多选删除），固定存在。实现多人一起听歌（新加入用户自动获取开始播放时间并跳转到对应播放时间），支持音乐格式包括flac等常用音乐格式）。一首歌播放完成后，自动加入原歌单的最后一位（即列表循环）。并支持选择随机（会自动打乱播放索引顺序）
