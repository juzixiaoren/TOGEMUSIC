帮我写提示词，我要用这个提示词给能对我项目进行操作的AI，让他帮我用前端vue(帮我写页面）（后端flask调用python代码），实现1.用户登录注册（使用hash保存密文作为密码）2.用户可以上传音乐（自动提取文件名和属性中歌手名字），上传后的显示框会显示提取的文件名作为歌名，歌手名字作为歌手（可以批量上传），这个显示框可以修改，用户可以自己修改歌名和歌手名（批量修改，并非传一个改一个，而是批量上传完后用户可以对一堆框进行修改（文件名.文件类型-歌名(默认文件名)-歌手名（后两个可以修改）））。然后用户点击确认后正式存储在本地，并存储进数据库。3.用户可以创建歌单（输入歌单名字），点击确认后存储进数据库。4.用户可以点进歌单详细页，可以导入歌曲（用户可以查看后台所有上传的歌曲（无论是自己上传还是别人上传），并可以根据（上传时间），上传用户名进行筛选，可以进行歌手/歌曲的搜索筛选，并勾选（可多选）最后点击确认将这些歌曲导入歌单（注意：切换筛选条件不会导致已经勾选的歌曲取消勾选）），此外，用户还可以查看所有歌单页，多选歌单导入本歌单（自动去重）。5.用户可以进入主房间（只有一个房间，做成固定页面，无需保存在数据库），并从歌单页or音乐页选择歌单(自动勾选歌单中全部音乐，可以一键取消勾选然后自己单选），选择音乐（同上面的筛选功能）加入正在播放的歌曲列表（正在播放歌单是数据库中的歌单表中的ID=1的表，不允许删除歌单（但是允许删除歌曲，可以多选删除），固定存在。实现多人一起听歌（新加入用户自动获取开始播放时间并跳转到对应播放时间），支持音乐格式包括flac等常用音乐格式）。一首歌播放完成后，自动加入原歌单的最后一位（即列表循环）。并支持选择随机（会自动打乱播放索引顺序）


你是一个全栈开发 AI，任务是帮我实现一个多人在线听歌项目，前端用 Vue 3，后端用 Flask + Python，数据库用 SQLite。项目功能需求如下：

1️⃣ 用户登录注册：

- 用户可以注册账号，密码必须用 hash（如 bcrypt 或 hashlib.sha256）存储为密文。
- 登录时验证 hash 密码。
- 前端登录/注册页面需要表单验证，密码长度≥6，用户名唯一。

2️⃣ 用户上传音乐：

- 用户可以批量上传音乐文件（mp3, flac 等常用格式）。
- 上传后，前端显示上传文件列表，每个文件显示：
  - 默认歌名：文件名（去掉后缀）
  - 默认歌手：文件属性中的歌手名（如果有）
- 用户可以在上传后对批量歌曲的歌名和歌手名进行修改（界面允许批量编辑，不是单个提交）。
- 用户确认后：
  - 文件保存到服务器本地目录
  - 歌曲信息写入数据库（包括歌名、歌手、文件路径、上传用户、上传时间、时长）

3️⃣ 歌单管理：

- 用户可以创建歌单（输入歌单名字），确认后写入数据库。
- 歌单详细页：
  - 可以导入歌曲：
    - 显示所有已上传的歌曲（无论上传者是谁）
    - 可按上传时间、上传用户名筛选
    - 可按歌手/歌名搜索
    - 可多选歌曲，确认后导入歌单
    - 已勾选的歌曲不会因为切换筛选条件而取消勾选
  - 可以导入其他歌单中的歌曲（多选歌单），自动去重

4️⃣ 主播放房间：

- 页面固定，无需保存房间信息到数据库
- 用户可以从歌单页或音乐页选择歌单：
  - 自动勾选歌单中全部歌曲，可取消全部勾选后自己单选
  - 点击确认后加入正在播放的歌曲列表
- 正在播放歌单固定为数据库中 ID=1 的歌单：
  - 不允许删除歌单，但允许删除歌曲（可多选）
- 支持多人同步播放：
  - 新加入用户自动获取当前播放时间并跳转到对应时间点
- 播放支持常用音乐格式（mp3, flac 等）
- 播放规则：
  - 一首歌播放完成后，自动加入原歌单的最后一位，实现列表循环
  - 支持选择随机播放（打乱播放顺序）

5️⃣ 数据库设计：

- 用户表：id, username, password_hash
- 歌曲表：id, title, artist, duration, file_path, uploader_id, time_added
- 歌单表：id, creator_id, playlist_name, UNIQUE(playlist_name)
- 歌单-歌曲关联表：playlist_id, song_id, order_index, UNIQUE(order_index, playlist_id)

如果数据库无法满足我的要求，你可以自行添加数据库类的功能或者自行修改数据库的结构

6️⃣ 前端要求：

- 使用 Vue 3 + Vue Router + Vuex（或 Pinia）管理状态
- 使用可视化表格/列表显示歌曲和歌单，可进行筛选、搜索、勾选
- 上传音乐界面支持批量文件拖拽上传
- 播放器支持播放进度同步、多用户同步

7️⃣ 后端要求：

- Flask 提供 REST API：
  - 用户注册/登录
  - 上传音乐并返回解析后的歌名、歌手
  - 保存修改后的歌曲信息
  - 创建歌单、查询歌单、导入歌曲
  - 播放列表管理（正在播放歌单）
- 支持 CORS，让前端 Vue 可访问
- 使用 SQLite 保存所有数据

请帮我生成：

1. 数据库初始化和 ORM/SQL 操作代码
2. Flask 后端接口代码
3. Vue 前端页面组件代码（登录/注册、上传音乐、歌单管理、主播放房间）
4. 文件上传处理和多用户同步播放逻辑

注意：

- 后端和前端要能联通，API 能直接调用数据库
- 所有路径/文件操作都要考虑服务器部署
- 播放器支持 flac 和 mp3
- 支持批量操作和批量编辑
- 播放列表循环和随机播放逻辑要在后端或前端都可实现

生成的代码要可直接运行，并且可以测试全部功能。
